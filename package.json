{
  "name": "consultorio-azabache",
  "private": true,
  "version": "0.1.0",
  "description": "Sitio web del ROGA Laboratorio médico y salud ocupacional (monorepo).",
  "scripts": {
    "prepare": "husky",
    "eslint": "npm run eslint --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
